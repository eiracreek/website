<section class="section breed-preview">
	<div class="container preview-grid">
		<div class="preview-copy">
			<p class="eyebrow">The Golden Guernsey</p>

			<h2>A Different Kind of Dairy Goat</h2>

			<p class="lead">
				Beautiful, intelligent, affectionate, and full of personality, Golden
				Guernseys have a way of leaving a lasting impression.
			</p>

			<p>
				Their flowing coats and warm golden coloring may capture attention first,
				but their gentle temperament, remarkable intelligence, and rich history are
				what inspire lifelong admiration. Discover why one of America's rarest
				heritage dairy goat breeds has earned such a devoted following.
			</p>

			<a class="button" href="/the-breed">Discover the Breed</a>
		</div>

		<div class="preview-image">
			<img
				src="/images/hero-goat.jpg"
				alt="Golden Guernsey goat at Eira Creek"
				loading="lazy"
			/>
		</div>
	</div>
</section>

<style>
	.preview-grid {
		display: grid;
		grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
		gap: clamp(3rem, 8vw, 7rem);
		align-items: center;
	}

	.preview-copy p:not(.eyebrow):not(.lead) {
		color: var(--color-text-soft);
	}

	.preview-copy .button {
		margin-top: 1rem;
	}

	.preview-image {
		overflow: hidden;
		min-height: 520px;
		border-radius: var(--radius-large);
		box-shadow: var(--shadow-soft);
	}

	.preview-image img {
		width: 100%;
		height: 100%;
		min-height: 520px;
		object-fit: cover;
		transition: transform 300ms ease;
	}

	.preview-image:hover img {
		transform: scale(1.03);
	}

	@media (max-width: 800px) {
		.preview-grid {
			grid-template-columns: 1fr;
		}

		.preview-image,
		.preview-image img {
			min-height: 380px;
		}
	}
</style>