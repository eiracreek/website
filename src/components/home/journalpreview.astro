<section class="section journal-preview">
	<div class="container">
		<div class="journal-heading">
			<div>
				<p class="eyebrow">The Eira Creek Journal</p>
				<h2>Always Learning.</h2>
			</div>

			<p class="lead">
				Every season brings new lessons. We share the experiences, observations, and
				decisions that continue to shape our breeding program.
			</p>
		</div>

		<div class="journal-card">
			<p class="journal-label">Coming Soon</p>

			<div>
				<h3>Stories, observations, and lessons from Eira Creek</h3>
				<p>
					The journal will include kidding seasons, herd management, breeding decisions,
					show experiences, breed history, and educational resources for established and
					future breeders.
				</p>
			</div>

			<a class="button button-secondary" href="/journal">Visit the Journal</a>
		</div>
	</div>
</section>

<style>
	.journal-preview {
		background: var(--color-secondary);
	}

	.journal-heading {
		display: grid;
		grid-template-columns: minmax(0, 0.8fr) minmax(0, 1.2fr);
		gap: clamp(2rem, 6vw, 6rem);
		align-items: end;
		margin-bottom: 3rem;
	}

	.journal-heading .lead {
		margin-bottom: 0;
	}

	.journal-card {
		display: grid;
		grid-template-columns: auto minmax(0, 1fr) auto;
		gap: 2rem;
		align-items: center;
		padding: clamp(2rem, 5vw, 3.5rem);
		border: 1px solid rgba(18, 58, 99, 0.12);
		border-radius: var(--radius-large);
		background: white;
		box-shadow: var(--shadow-soft);
	}

	.journal-card p {
		margin-bottom: 0;
		color: var(--color-text-soft);
	}

	.journal-label {
		padding: 0.55rem 0.8rem;
		border-radius: 999px;
		background: var(--color-neutral);
		color: var(--color-dark);
		font-size: 0.72rem;
		font-weight: 800;
		letter-spacing: 0.12em;
		text-transform: uppercase;
		white-space: nowrap;
	}

	@media (max-width: 800px) {
		.journal-heading,
		.journal-card {
			grid-template-columns: 1fr;
		}
	}
</style>