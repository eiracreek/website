<section class="section section-neutral herd-preview">
	<div class="container preview-grid">
		<div class="herd-feature">
			<p class="herd-feature-label">Our Breeding Program</p>

			<p class="herd-feature-quote">
				Every breeding decision is guided by a single responsibility: to strengthen the
				Golden Guernsey without sacrificing the qualities that make it unique.
			</p>
		</div>

		<div class="preview-copy">
			<p class="eyebrow">Our Herd</p>

			<h2>Raised With Purpose. Loved Every Day.</h2>

			<p>
				Every goat at Eira Creek reflects our commitment to the future of the Golden
				Guernsey. We bottle-raise every kid from birth, giving each one the opportunity to
				build trust through daily care, gentle handling, and time spent with people.
			</p>

			<p>
				Combined with responsible breeding, that thoughtful husbandry helps us raise
				healthy, productive, structurally correct goats that remain true to the breed’s
				historic character.
			</p>

			<a class="button" href="/our-herd">Meet Our Herd</a>
		</div>
	</div>
</section>

<style>
	.preview-grid {
		display: grid;
		grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
		gap: clamp(3rem, 8vw, 7rem);
		align-items: center;
	}

	.preview-copy p:not(.eyebrow) {
		color: var(--color-text-soft);
	}

	.preview-copy .button {
		margin-top: 1rem;
	}

	.herd-feature {
		position: relative;
		display: flex;
		min-height: 480px;
		flex-direction: column;
		justify-content: flex-end;
		padding: clamp(2rem, 5vw, 4rem);
		overflow: hidden;
		border-radius: var(--radius-large);
		background:
			linear-gradient(
				0deg,
				rgba(18, 58, 99, 0.96),
				rgba(18, 58, 99, 0.52)
			),
			url('/images/hero-goat.jpg') center / cover no-repeat;
		box-shadow: var(--shadow-soft);
		color: white;
	}

	.herd-feature-label {
		margin-bottom: 1rem;
		color: var(--color-accent);
		font-size: 0.78rem;
		font-weight: 800;
		letter-spacing: 0.18em;
		text-transform: uppercase;
	}

	.herd-feature-quote {
		margin-bottom: 0;
		color: white;
		font-family: var(--font-heading);
		font-size: clamp(1.55rem, 3vw, 2.45rem);
		line-height: 1.35;
	}

	@media (max-width: 800px) {
		.preview-grid {
			grid-template-columns: 1fr;
		}

		.herd-feature {
			min-height: 400px;
		}
	}
</style>